G4系列
0 ~ 9页        为bootloader代码区

10页  0 ~ 31    为升级标志位
     32 ~ 63    为程序版本号
     64 ~ 95    为设备id号
     96 ~ 127   为设备默认值是否写入
     128 ~ 159  为设备id号
     160 ~ 191  为can发送频率
     192 ~ 223  为编码器方向
     224 ~ 255  为编码器原点
     256 ~ 287  为iap升级标志

     11页内容属于默认参数，如果需要修改的话，需要先进进行读取，将参数保存下来，之后在进行擦除修改
     bootloader程序阶段，只需要进行读取，所以影响不大
     app执行阶段，涉及到修改编码器方向，以及原点，所以需要注意进行保存参数，防止数据丢失

程序区域根据实际使用设置
现有关节和手柄代码大小为15k以内，底程序在64k ，加上所以内存暂定


canfd接收固定格式
帧头  0x02 0xfd
大小  0x00 0x30
报文类型    0x03
指令id -->     握手     0x01
      |
      v
      -->     数据     0x02
      v
      |
      -->     结束     0x03
帧尾  0x03 0xfc

OTA举例
头帧		  |  报文长度		设备编号	报文类型	报文版本	时间是否同步	应答位	任务序号		指令ID													校验位		尾帧
0x02 0xfd |	0x00 0x12	0x01	0x03	0x02	0x11
          |







